using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace TriagingSystem.Services
{
    public class classHandler
    {
    }

    /* 
     // GET: api/Todo/5
        [HttpGet("{id}")]
        public async Task<ActionResult<Student>> GetStudent(int id)
        {
            var s = await _db.Student.FindAsync(id);

            if (s == null)
            {
                return NotFound();
            }

            return s;
        }

        // POST api/<controller>
        [HttpPost]
        public async Task<ActionResult<Student>> PostTodoItem([FromBody]Student item)
        {
            _db.Student.Add(item);
            await _db.SaveChangesAsync();

            return CreatedAtAction(nameof(GetStudent), new { id = item.Id }, item);
        }
 

     
     */
}
